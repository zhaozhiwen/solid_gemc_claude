# solid_gemc_claude workspace — generated artifacts

# Per-project run output (one runs/ subdir per project)
*/runs/*/

# Built upstream tree
solid_gemc/

# Plugin-managed heavy artifacts, anchored to this workspace so they survive
# plugin updates (the 1.7 GB .sif and the analysis venv)
cache/
venv/

# File-type-level catches
*.root
*.hipo
*.evio

# Python / Jupyter
__pycache__/
.venv/
.ipynb_checkpoints/
